{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "studio",
  "description": "Minimal local Studio capability",
  "windows": ["studio"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
